#include <iostream>

int main() //main function
{
    // int slices = 5; variable are used to store a value. DataType int, identifier slices, assignment operator =, value 5.

    int slices; //declaration, it says that that identifier exist.
    slices = 5 + 1; //initialization, it says that it has a value.

    int children = slices;

    std::cout << slices;
    return 0;
}